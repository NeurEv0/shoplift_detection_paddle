name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      # Expose the vendored PaddleDetection source so `import ppdet` resolves
      # without a (brittle) editable install; see requirements.txt header note.
      PYTHONPATH: ${{ github.workspace }}/src/PaddleDetection-release-2.9
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Environment self-check
        run: python scripts/check_env.py

      - name: Lint (ruff)
        run: ruff check shoplift scripts

      - name: Run unit tests
        run: python -m pytest shoplift/tests -q
